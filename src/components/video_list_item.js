import React from 'react';

// test return of VideoListItem
const VideoListItem = (props) => {
  // variable video to pull off video from props object
  const video = props.video
  return <li>Video</li>
}

export default VideoListItem;